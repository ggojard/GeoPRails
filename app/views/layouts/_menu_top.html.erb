<% if current_admin_user != nil %>
<div class="navbar navbar-default navbar-static-top main-menu" role="navigation" ng-controller="MenuCtrl">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      </button>
      <a class="logo" href="/"><img src="/images/logo_small.png"></a>
      <!-- <a class="navbar-brand" href="/">{{company.name}}</a> -->
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        
        
        <li class="dropdown" ng-repeat="b in company.buildings">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">{{b.name}} <b class="caret"></b></a>
          <ul class="dropdown-menu">
            <li ng-repeat="f in b.floors"><a href="/floors/{{f.id}}">{{f.name}}</a></li>
            <li class="divider"></li>
            <li><a href="{{b.url}}">{{b.name}}</a></li>
            <li><a href="{{b.url}}/export">Exporter (xslx)</a></li>
          </ul>
        </li>
        <% if @global_user_type != 'READ' %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">Catalogue <b class="caret"></b></a>
          <ul class="dropdown-menu">
            <li><a href="/admin/buildings">Bâtiments</a></li>
            <li class="divider"></li>
            <li><a href="/admin/organizations">Organisations</a></li>
            <li><a href="/people">Personnes</a></li>
            <li><a href="/items">Objects</a></li>
          </ul>
        </li>
        <li class=""><a href="/admin">Base de donnée</a></li>
        <% end %>
        <li  class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= current_admin_user.email %><b class="caret"></b></a>
          <ul class="dropdown-menu">
            <li><a href="/admin/logout">Fermer la session</a></li>
          </ul>
        </li>
      </ul>
      <%=  render '/homes/search' %>
      </div><!--/.nav-collapse -->
    </div>
  </div>
  <% end %>
